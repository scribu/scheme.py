(define PI2 3.41)

(display (cdr (list 1 1 2 #t #f "foo" (quote PI2) PI2)))

(define ! (lambda (n)
    (if (>= 1 n)
        1
        (* n (! (- n 1))))))

(display (string-concatenate (list
	"\nfactorial: "
	(number->string (* 0.3 (! 10))))))
