(define PI2 3.41)

(define factorial (lambda (n)
    (if (>= 1 n)
        1
        (* n (factorial (- n 1))))))

(display (string-concatenate (list
	"result: "
	(number->string (* 0.3 (factorial 10))))))

(display (cdr (list 1 2 3 "foo")))

(display (list #t #f (quote PI2) PI2))
